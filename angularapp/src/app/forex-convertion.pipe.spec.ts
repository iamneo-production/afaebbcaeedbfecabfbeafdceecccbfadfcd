import { ForexConvertionPipe } from './forex-convertion.pipe';

describe('ForexConvertionPipe', () => {
  it('create an instance', () => {
    const pipe = new ForexConvertionPipe();
    expect(pipe).toBeTruthy();
  });
});
